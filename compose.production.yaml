services:
  sales-copilot:
    image: ${IMAGE_NAME:-ghcr.io/your-org/newtonx-sales-copilot:latest}
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
